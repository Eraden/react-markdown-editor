import * as types from "../reducers/types";

export const editorTabClicked  = () => ({
    type: types.EDITOR_TAB_CLICKED
});
export const previewTabClicked = () => ({
    type: types.PREVIEW_TAB_CLICKED
});
